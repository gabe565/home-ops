apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: longhorn
  namespace: longhorn-system
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      namespace: kgateway-system
      name: public
  hostnames:
    - ${app_url}
  rules:
    - backendRefs:
        - kind: Service
          name: longhorn-frontend
          port: 80
---
apiVersion: gateway.kgateway.dev/v1alpha1
kind: GatewayExtension
metadata:
  name: auth
  namespace: longhorn-system
spec:
  type: OAuth2
  oauth2:
    backendRef:
      name: authentik-server
      namespace: authentik
      port: 80
    issuerURI: https://${oidc_host}/application/o/longhorn/
    credentials:
      clientID: ${oidc_client_id}
      clientSecretRef:
        name: oidc
    scopes: [openid, offline_access]
---
apiVersion: gateway.kgateway.dev/v1alpha1
kind: TrafficPolicy
metadata:
  name: auth
  namespace: longhorn-system
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: longhorn
  oauth2:
    extensionRef:
      name: auth
