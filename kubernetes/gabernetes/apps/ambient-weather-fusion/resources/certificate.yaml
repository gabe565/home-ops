apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mqtt-tls
  namespace: ambient-weather-fusion
spec:
  secretName: mqtt-tls
  commonName: ambient_weather_fusion
  duration: 8760h # 1 year
  renewBefore: 720h # 30 days
  issuerRef:
    kind: ClusterIssuer
    name: mqtt
