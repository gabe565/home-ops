apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgresql
  namespace: vikunja
spec:
  instances: 3
  imageName: ghcr.io/cloudnative-pg/postgresql:16.3-9
  enableSuperuserAccess: true
  bootstrap:
    initdb:
      database: vikunja
      owner: vikunja
  storage:
    storageClass: local-path
    size: 8Gi
  resources:
    requests:
      memory: 20Mi
      cpu: 10m
  smartShutdownTimeout: 1
  stopDelay: 300
